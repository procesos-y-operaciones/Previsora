<p id="notice"><%= notice %></p>

<h1>Type Processes</h1>
<p>
Download:
  <%= link_to "Excel", type_processes_path(format: "xls") %>
</p>
<!--
<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @type_processes.each do |type_process| %>
      <tr>
        <td><%= link_to 'Show', type_process %></td>
        <td><%= link_to 'Edit', edit_type_process_path(type_process) %></td>
        <td><%= link_to 'Destroy', type_process, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Type Process', new_type_process_path %>
-->
