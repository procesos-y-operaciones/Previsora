<%= form_with(model: attorny, local: true) do |form| %>
  <% if attorny.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(attorny.errors.count, "error") %> prohibited this attorny from being saved:</h2>

      <ul>
      <% attorny.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="offset-lg-2 col-lg-8 ">


        <%= form.label :name, "Nombre del Abogado Externo" %>
        <div class="input-group">
          <%= form.text_field :name, id: :attorny_name, placeholder: "NOMBRE", class:"form-control" %>
        </div>

      <%= form.submit "Guardar", class:"btn btn-default floatRight" %>

    </div>
  </div>
<% end %>
