<!--div class="firtsHeader"></div-->
<div class="container-fluid">
  <div class="row flexBox">
    <div class="col-xs-6 p-30 flexBox verticalCenter">
      <h4 class="justify m-30 lh-150 ls-1"> 
        Apreciado usuario, bienvenido al <b class="ligthgreen">Registro Único de Casos</b> de la Gerencia de Procesos Judiciales.
        <br><br>
        Contamos con su compromiso y gran apoyo en la <b class="ligthgreen">verificación y validación de la información </b> 
        que deba registrar o actualizar en el desarrollo de su gestión. De su colaboración depende el éxito de la gestión 
        de la Gerencia de Procesos Judiciales.
      </h4>
    </div>
    <div class="col-xs-6">
      <h2 class="">Iniciar Sesión</h2>

      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

        <div>
          <%= f.label :documento, class:"form-label" %><br />
          <%= f.text_field :document, autofocus: true, class:"form-control" %>
        </div>
        <br>
        <div>
          <%= f.label :contraseña, class:"form-label" %><br />
          <%= f.password_field :password, autocomplete: "off", class:"form-control" %>
        </div>

        <% if devise_mapping.rememberable? -%>
          <div class="field">
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me, class:"" %>
          </div>
        <% end -%>

        <div class="actions">
          <%= f.submit "Iniciar Sesión", class:"btn btn-default fw" %>
        </div>
      <% end %>

      <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
        <%= link_to "¿Olvidaste tu contraseña?", new_password_path(resource_name), class:"link" %><br />
      <% end -%>
      <br>
    </div>
  </div>
</div>
