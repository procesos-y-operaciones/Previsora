<!--div class="firtsHeader"></div-->
<div class="container-fluid">
  <div class="row flexBox">
    <div class="col-xs-6 p-30 flexBox verticalCenter">
      <p class="justify"> 
        Apreciado usuario, bienvenido al Registro Único de Casos de la Gerencia de Procesos Judiciales.
        <br><br>
        Contamos con su compromiso y gran apoyo en la verificación y validación de la información que deba registrar o actualizar en el desarrollo 
        de su gestión. De su colaboración depende el éxito de la gestión de la Gerencia de Procesos Judiciales.
      </p>
    </div>
    <div class="col-xs-6">
      <h2 class="">Iniciar Sesión</h2>

      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

        <div>
          <%= f.label :documento, class:"form-label" %><br />
          <%= f.text_field :document, autofocus: true, class:"form-control" %>
        </div>
        <br>
        <div>
          <%= f.label :contraseña, class:"form-label" %><br />
          <%= f.password_field :password, autocomplete: "off", class:"form-control" %>
        </div>

        <% if devise_mapping.rememberable? -%>
          <div class="field">
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me, class:"" %>
          </div>
        <% end -%>

        <div class="actions">
          <%= f.submit "Iniciar Sesión", class:"btn btn-default fw" %>
        </div>
      <% end %>

      <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
        <%= link_to "¿Olvidaste tu contraseña?", new_password_path(resource_name), class:"link" %><br />
      <% end -%>
      <br>
    </div>
  </div>
</div>
